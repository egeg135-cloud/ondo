# 의존성
node_modules
dist
dist-ssr

# 환경변수 (절대 커밋 금지 — anon key 등 민감정보 포함)
.env
.env.local
.env.*.local

# 로그
*.log
npm-debug.log*

# 에디터/OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# 옛 프로토타입 백업 (배포 레포에서 제외)
src_old_backup/
.vercel
